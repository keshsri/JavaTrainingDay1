public class practice {
    public static void main(String[] args){
        int x =50, y = 5;
        int add,sub,mul,div,mod;

        add = x + y;
        sub = x - y;
        mul = x * y;
        div = x / y;
        mod = x % y;

        System.out.printf("%d, %d, %d, %d, %d",add,sub,mul,div,mod);
    }
}
